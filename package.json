{
  "name": "progreso-shore-excursions",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "build": "python3 scripts/assemble-pages.py",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  }
}
